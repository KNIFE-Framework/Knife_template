# 📈 8. GitHub Actions a Deploy

```bash
npm install --save-dev gh-pages
```

V `package.json`:
```json
"scripts": {
  "deploy": "docusaurus deploy"
}
```

Deploy cez:
```bash
npm run deploy
```

[Home](../01-Instalacia-Docusaurus.md)